export interface User {
  id: string;        // Уникальный идентификатор
  name: string;      // Имя пользователя
  avatar: string;    // Ссылка на картинку аватара
  createdAt: string; // Дата создания (строка ISO)
}